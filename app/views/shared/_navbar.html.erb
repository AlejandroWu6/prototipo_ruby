<nav class="navbar navbar-expand-lg navbar-primary bg-primary mb-4">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand text-white fw-bold" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "About Me", about_path, class: "nav-link text-white" %>
        </li>
      </ul>
      <% if controller_name == "registrations" && action_name == "new" %>
        <div class="ms-auto">
          <%= link_to "Login", login_path, class: "btn btn-outline-light" %>
        </div>
      <% else %>
        <% if session[:user_id] %>
          <% @user = User.find_by(id: session[:user_id]) rescue nil %>
          <% if @user %>
        <span class="navbar-text text-white ms-auto">
          Hi, <%= @user.email.split('@').first %>
        </span>
          <% end %>
        <% else %>
          <div class="ms-auto">
        <%= link_to "Sign Up", sign_up_path, class: "btn btn-outline-light me-2" %>
        <%= link_to "Login", login_path, class: "btn btn-outline-light" %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
